<Page
    x:Class="VikingsGame.View.HarborView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VikingsGame.View"
    xmlns:vm="using:VikingsGame.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.DataContext>
        <vm:HarborVM></vm:HarborVM>
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid Grid.ColumnSpan= "3">
            <Grid.RowDefinitions>
                <RowDefinition Height="140"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>

            <Grid Column="1" Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="4*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>
                <ListView Grid.Column="0" ItemsSource="{Binding Ships}" SelectedItem="{Binding SelectedShip, Mode=TwoWay}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <StackPanel Grid.Column="1" Margin="0">
                    <Grid Margin="0,20,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.ColumnSpan="2">
                            <TextBlock Text="{Binding SelectedShip.Name}" Style="{StaticResource SubheaderTextBlockStyle}" Margin="0,0,0,20"/>
                            <TextBlock Text="About this ship:"/>
                            <TextBlock Text="{Binding SelectedShip.TravelDescription}" TextWrapping="Wrap" Margin="0,0,0,15"></TextBlock>

                            <Grid Margin="0,0,0,25">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel  Grid.Column="1"  Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Button Grid.Column="3" Name="cmdUp" Margin="0,0,0,0" Content="˄" Command="{Binding IncOrder}" FontSize="12" />
                                    <Button Grid.Column="2" Name="cmdDown" Margin="0,0,0,0"  Content="˅" Command="{Binding DecOrder}" FontSize="12" />
                                </StackPanel>

                                <StackPanel VerticalAlignment="Center" Grid.ColumnSpan="2" Margin="0,13">
                                    <TextBlock>
                                    <Run Text="Units on ship" />
                                    <Run Text="{Binding SelectedShip.Units.UnitCount}"/>
                                    </TextBlock>
                                    <TextBlock>
                                    <Run Text="Available units in city" />
                                    <Run Text="{Binding CurrentCity.Units.UnitCount}"/>
                                    </TextBlock>
                                </StackPanel>
                            </Grid>


                            <TextBlock Text="Stats:"/>
                            <ItemsControl ItemsSource="{Binding Stats}" BorderThickness="0" Margin="0">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Width="200" HorizontalAlignment="Left">
                                            <TextBlock Text="{Binding Key}" HorizontalAlignment="Left"/>
                                            <TextBlock Text="{Binding Value}" HorizontalAlignment="Right"/>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <!--<TextBlock Text="Stats of selected upgrade"/>
                            <ItemsControl ItemsSource="{Binding UpgradeStats}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Width="200" HorizontalAlignment="Left">
                                            <TextBlock Text="{Binding Key}" HorizontalAlignment="Left"/>
                                            <TextBlock Text="{Binding Value}" HorizontalAlignment="Right"/>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>-->
                            <TextBlock Text="Built upgrades" Margin="0,15,0,0"/>
                            <ListView Grid.Column="0" ItemsSource="{Binding BuiltUpgrades}" SelectedItem="{Binding SBuiltUpgrade, Mode=TwoWay}">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Width="200" HorizontalAlignment="Left">
                                            <TextBlock Text="{Binding Name}" HorizontalAlignment="Left"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                            <TextBlock Text="Available upgrades"/>

                            <ListView Grid.Column="0" ItemsSource="{Binding AvailableUpgrades}" SelectedItem="{Binding SAvailableUpgrade, Mode=TwoWay}">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Width="200" HorizontalAlignment="Left">
                                            <TextBlock Text="{Binding Name}" HorizontalAlignment="Left"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                            <Button Content="Build upgrade" Command="{Binding BuildUpgradeClick}" Margin="0,15,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                        </StackPanel>
                    </Grid>
                </StackPanel>
                <StackPanel Grid.Column="2" >
                    <ListView ItemsSource="{Binding Cities}" SelectedItem="{Binding SelectedCity, Mode=TwoWay}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <TextBlock Text="{Binding Id}"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <Button Content="Send ship to selected city" Command="{Binding SailToClick}"/>
                </StackPanel>
            </Grid>


            <Button Name="BackButton" Margin="39,59,39,0"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button" />

            <TextBlock x:Name="PageTitle" Text="TODO: DESIGN" Style="{StaticResource HeaderTextBlockStyle}" Grid.Row="0"
                       Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>
        </Grid>

    </Grid>
</Page>

